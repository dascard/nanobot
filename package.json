{
  "dependencies": {
    "@tavily/core": "^0.7.2"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.2",
    "@commitlint/config-conventional": "^20.5.0",
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky",
    "test": "python -m pytest tests/ -v"
  }
}
