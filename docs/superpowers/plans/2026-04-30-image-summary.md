# 图片摘要工具实现计划

> 目标：新增一个显式的图片摘要工具，支持视觉模型结构化输出，并让主对话模型在能识图时可直接给出结构化摘要。

## 计划

- [ ] 先补测试，覆盖工具元数据、视觉模型选择、JSON 输出和手动模型路由
- [ ] 实现 `image_summary` 工具及 KT 导入桥接，直接调用本地 Qwen 视觉接口
- [ ] 更新配置、提示词和输出进度文案，补足可调参数
- [ ] 跑回归测试并修复失败项

## 约束

- 不把图片摘要做成每条消息的强制前置步骤
- 不改变现有多模态聊天主链路
- 不把摘要结果自动写入历史，先保留为显式工具能力
